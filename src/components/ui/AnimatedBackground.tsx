import React from 'react';

// Neutralized — the editorial pass uses a single static wash on Index.tsx.
// Kept as a no-op so any lingering imports continue to build cleanly.
const AnimatedBackground: React.FC<{ variant?: 'default' | 'subtle' | 'vibrant' }> = () => null;

export default AnimatedBackground;
